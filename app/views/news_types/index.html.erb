<h1>Listing news_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Cache slug</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news_types.each do |news_type| %>
  <tr>
    <td><%= news_type.name %></td>
    <td><%= news_type.cache_slug %></td>
    <td><%= link_to 'Show', news_type %></td>
    <td><%= link_to 'Edit', edit_news_type_path(news_type) %></td>
    <td><%= link_to 'Destroy', news_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News type', new_news_type_path %>
