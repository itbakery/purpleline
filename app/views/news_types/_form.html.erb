<%= form_for(@news_type) do |f| %>
  <% if @news_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_type.errors.count, "error") %> prohibited this news_type from being saved:</h2>

      <ul>
      <% @news_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cache_slug %><br />
    <%= f.text_field :cache_slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
