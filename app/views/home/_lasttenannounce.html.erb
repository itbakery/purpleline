<ul>
    	<%if session[:lang]=="en" %>
     	 <h2>Latest Announcement</h2>
      <%end%>
      <%if session[:lang]=="th" %>
     	 <h2>ประกาศล่าสุด</h2>
      <%end%> 

     <%@lasttenannounces.each do |announce|%>
      <li class=<%=cycle('even', 'odd')%>
           <span style="font-size:14px;">
           <a href="/announce/<%=announce.id%>"><%=announce.title%>
           </span>
           
           <i style="float:right;"> start on <%= announce.start_on.to_date.to_s(:long) %> </i>
      </li> 
     <%end%>
</ul>
