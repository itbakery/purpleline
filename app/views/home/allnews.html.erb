
<ul>
    	<%if session[:lang]=="en" %>
     	 <h2 style="margin-top:15px;">All Newsletters</h2>
      <%end%>
      <%if session[:lang]=="th" %>
     	 <h2 style="margin-top:15px;">ข่าวสารทั้งหมด</h2>
      <%end%> 

<% @allnewletters.each do |newsletter|%>
    <li class=<%=cycle('even', 'odd')%>
        <span style="font-size:14px;">
          <a href="/shownews/<%=newsletter.id%>"><%=newsletter.title%>
       </span>  
                  <i style="float:right;"> <%= newsletter.start_on.to_date.to_s(:long) %></i>
            		</li> 
 <%end%>
</ul>
