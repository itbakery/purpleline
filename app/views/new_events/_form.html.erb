<%= form_for(@new_event) do |f| %>
  <% if @new_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_event.errors.count, "error") %> prohibited this new_event from being saved:</h2>

      <ul>
      <% @new_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cached_slug %><br />
    <%= f.text_field :cached_slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
